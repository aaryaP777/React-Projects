import React from "react";

import {Button} from "reactstrap";

function MyButton() {

    return (
        <div>
            <button color = "primary" onClick={bth}>Click me</button>
        </div>
    )
}

export default MyButton
